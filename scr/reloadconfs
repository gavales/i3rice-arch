#!/bin/bash

bash $HOME/xrs/xrs-reload
$HOME/scr/themer -t "$HOME/git/i3rice-arch" -x " "
$HOME/scr/themer -t "$HOME/.vim/colors" -x " "
$HOME/scr/themer -t "$HOME/git/atompackages/packages/rice-syntax/styles" -x " "
$HOME/scr/themer -t "$HOME/scr" -x " "
$HOME/scr/themer -t "$HOME/.config/vis/colors" -x " "
$HOME/scr/themer -t "$HOME/.config/rofi" -x " "
$HOME/scr/themer -t "$HOME/plt/css" -x " "
$HOME/scr/themer -t "$HOME/.config/zathura" -x " "
$HOME/scr/themer -t "$HOME/.config/dunst" -x "dunst-reload"
$HOME/scr/i3concat
killall -q polybar
polybar i3 &>/dev/null &
$HOME/scr/i3listen-toggle
