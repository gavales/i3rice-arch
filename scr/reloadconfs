#!/usr/bin/env bash

WPP="$(themer -v 'wallpaper ')"
TNT="$(themer -v 'tint ')"
[[ -n $WPP ]] || WPP=$(themer -v 'acc ')
[[ -n $TNT ]] && TINT="-t $TNT" || TINT=" "
gwc -w "$WPP" $TINT
setxkbmap "$(themer -v xkb)"
[[ -n $(pidof cwm) ]] && gwmrel --toggle
themer --apply-all
