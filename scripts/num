#!/bin/bash

stat=$(xset -q | grep 'Caps Lock:' | awk '{print $8}')

if [[ $stat == "on" ]]; then
    echo "<span background=\"$(bash ~/scripts/getxcolors magenta)\" foreground=\"$(bash ~/scripts/getxcolors black)\"> NUM LOCK </span>"
else
    :
fi
