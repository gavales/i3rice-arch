#!/bin/bash

playing=$(mpc current)

if [ -z "$playing" ]
then
	:
else
	echo "<span face='Font Awesome 5 Free'>&#xf001;</span> ${playing}"
fi

case $BLOCK_BUTTON in
  5) mpc prev ;;
  1) mpc toggle ;;
  4) mpc next ;;
esac
