#!/bin/bash

# OA: Gavin Vales
# CREATED: Tue 11 Jun 14:48:08 BST 2019
declare -A TMR
eval "$(themer --list varsh)"

XDIR=$HOME/xrs
XRS=$HOME/.Xresources
cat ".themer/${TMR[name]}/.Xresources" | sed "s/\%\%.*\%\%//g" > $XRS
xrdb -load $XRS
kill -1 $(pidof urxvtd)
pidof st | xargs kill -SIGUSR1
