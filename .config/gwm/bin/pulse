#!/usr/bin/env bash
#
# make the current window border "pulse" a la xero

FREQ=${FREQ:-0.05}
COLORS="$(cat pulsecolors | sed '/^#/d')"
CUR=$(pfw)
echo $COLORS

while :; do
	for c in $COLORS; do
		chwb -c ${c} $(pfw)
		sleep $FREQ
	done
done
