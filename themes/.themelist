#!/bin/bash

# OA: Gavin Vales
# CREATED: Sun  9 Jun 17:44:48 GMT 2019

main () {
	rm .theme_list
	thms=$(ls -l | grep -v "^total" | awk '{print $NF}')
	for i in $thms; do
		fg0=$(cat ${i} | grep "fg0" | awk '{print $NF}')
		acc=$(cat ${i} | grep "acc" | awk '{print $NF}')
		bg0=$(cat ${i} | grep "bg0" | awk '{print $NF}')
		echo "\
<span foreground=\"$bg0\" background=\"$fg0\"> fg </span>\
<span foreground=\"$fg0\" background=\"$bg0\"> bg </span>\
<span foreground=\"$fg0\" background=\"$acc\"> acc </span>\
 $i" >> .theme_list
	done
}

main
#red=$(cat ${i} | grep "red" | awk '{print $NF}')
#grn=$(cat ${i} | grep "grn" | awk '{print $NF}')
#ylw=$(cat ${i} | grep "ylw" | awk '{print $NF}')
#blu=$(cat ${i} | grep "blu" | awk '{print $NF}')
#gnt=$(cat ${i} | grep "gnt" | awk '{print $NF}')
#cyn=$(cat ${i} | grep "cyn" | awk '{print $NF}')
#<span foreground=\"#666666\" background=\"$com\"> com </span>\
#<span foreground=\"#666666\" background=\"$bkl\"> grey </span>\
#<span foreground=\"#666666\" background=\"$red\"> r </span>\
#<span foreground=\"#666666\" background=\"$grn\"> g </span>\
#<span foreground=\"#666666\" background=\"$ylw\"> y </span>\
#<span foreground=\"#666666\" background=\"$gnt\"> m </span>\
#<span foreground=\"#666666\" background=\"$cyn\"> c </span>\
